machine:
  services:
    - docker
dependencies:
  override:
    - sudo pip install --upgrade docker-compose
test:
  pre:
    - docker-compose build
  override:
    - pwd ~
    - ifconfig
    - echo HelloWorld
    - exit 0
