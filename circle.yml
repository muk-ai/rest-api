machine:
  python:
    version: 3.5.0
dependencies:
  override:
    - pip install -r ./falcon/requirements.txt
    - |
      uwsgi \
        --master \
        --http :3031 \
        --wsgi hello:application \
        --python-path /home/ubuntu/rest-api/falcon/ \
        --processes 2 \
        --daemonize /home/ubuntu/rest-api/falcon/uwsgi.log
test:
  override:
    - pwd
    - ip a
    - py.test /home/ubuntu/rest-api/falcon/test/test_hello.py
    - exit 0
